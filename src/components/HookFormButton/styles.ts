import { StyleSheet } from 'react-native'

export const hookFormButtonStyles = StyleSheet.create({
  button: {
    height: 45,
    marginTop: 28,
    marginHorizontal: '10%',
    justifyContent: 'center',
  },
})
